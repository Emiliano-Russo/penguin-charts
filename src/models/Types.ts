export interface userData {
  label: string;
  data: number[];
  backgroundColor: string;
}

export interface Date {
  Day: string;
  Month: string;
  Year: string;
}

export enum dateType {
  Year,
  Month,
  Day,
}
